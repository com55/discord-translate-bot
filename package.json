{
  "name": "discord-translate-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "register": "node --experimental-strip-types scripts/register.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "discord-interactions": "^4.1.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "@types/node": "^22.19.21",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0",
    "wrangler": "^4.100.0"
  }
}
